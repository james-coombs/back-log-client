<div class="col-sm-12 back-logs-table">
  <div>
    {{#each log}}
    <div class="row">
      <div class="col-sm-2">
        <h6>{{title}}</h6>
      </div>
      <div class="col-sm-2">
        <h6>{{platform}}</h6>
      </div>
      <div class="col-sm-2">
        <h6>{{players}}</h6>
      </div>
      <div class="col-sm-2">
        <h6>{{stage}}</h6>
      </div>
      <div class="col-sm-2">
        <h6>{{notes}}</h6>
      </div>
      <div class="col-sm-2 pull-right">
        <button class="full-width delete-log" id="{{id}}" data-control="deleteLog">Delete</button>
        <br>
        <hr class="no-margin">
        <button class="full-width update-log" id="{{id}}" data-control="updateLog">Update</button>
        <hr class="no-margin">
      </div>
      </div>
    {{/each}}
  </div>
</div>
